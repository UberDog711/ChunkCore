
0 Top
1 Bottom
2 Left
3 Right
4 Front
5 Back


Structure:
+ Mean Changes


src
    GameManager + (keep addition limited instead relly on calls to classes helpers)
    Main + (accomplish this all without adding more clutter)
    Constants + (keep minimal)
    tools
        ListenerAny
    player
        Player + (make it store pos but get changes from actions)
        InputManager + (isolate from player to just call actions)
        Actions + (compute the affect all action have such as movement)
    world
        Chunk + (adapt to new data structure)
            Vector3 + (remove and use arrays)
        WorldManager + (bit manipulable to store data)
    render
        Renderer + (put gl calls to move internally)
    reporting
        Util + (write data to csv file)

